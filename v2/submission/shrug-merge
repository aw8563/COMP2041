#!/bin/dash
if [ ! -d ".shrug" ]; then
	echo "shrug-merge: error: no .shrug directory containing shrug repository exists"
	exit 1
fi

# merge changes from target INTO current branch
target=$1

# commit msg
msg=$3


# check for merge conflicts somehow?????


